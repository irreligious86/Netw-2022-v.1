import React from 'react';
import classes from './dialogs.module.css';

const Dialogs = (props) => {
  return (
    <footer className={classes.dialogs}>
      Dialogs
    </footer>
  )
}

export default Dialogs;